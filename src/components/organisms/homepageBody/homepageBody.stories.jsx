import React from 'react';
import HomepageBody from './homepageBody';

export default {
  title: 'Organisms/Homepage Body',
  component: HomepageBody,
  argTypes: {},
};

function Template() {
  return (<HomepageBody />);
}

export const Default = Template.bind({});
